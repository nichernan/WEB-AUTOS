# Configuración de despliegue de Cloudflare Workers (Static Assets).
# El proyecto "web-autos" es un Worker (no Pages): sirve los archivos
# estáticos de PaginaToto-WEB/ (la app completa con login + Supabase).
name = "web-autos"
compatibility_date = "2024-09-23"

[assets]
directory = "PaginaToto-WEB"
